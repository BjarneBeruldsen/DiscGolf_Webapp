# Backend miljøvariabler
# Kopier denne filen til .env og fyll ut med dine egne verdier

# MongoDB Database connection
MONGODB_URI=mongodb://localhost:27017/discgolf

# Server konfigurasjon
PORT=8000

# NODE_ENV=production

# Session
SESSION_SECRET=ditt-session-secret-her
# Generer en sikker secret med: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# API Base URL (brukes av backend for CORS og Socket.IO konfigurasjon)
# Merk: Frontend trenger også denne variabelen i frontend/.env
# REACT_APP_API_BASE_URL=https://din-app.herokuapp.com

# E-post konfigurasjon (for kontaktskjema)
EPOST_BRUKER=din-epost@gmail.com
EPOST_PASSORD=ditt-app-passord

# For lokal utvikling, endre til:
NODE_ENV=development
REACT_APP_API_BASE_URL=http://localhost:8000
